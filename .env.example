# Server
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# Database (SQLite file path for dev, swap to Postgres connection string for prod)
DATABASE_URL="file:./dev.db"

# Integrations (add vendor API keys here as needed in later phases)
# TURNO_API_KEY=
# TURNO_API_URL=https://api.turno.com/v1
